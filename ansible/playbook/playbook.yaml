---
- name: Serial tasks
  hosts: working_hosts
  gather_facts: false
  become: true
  serial: 1
  roles:
     - role: prepare
       tags: ['prepare']

- name: Parallel tasks
  hosts: working_hosts
  gather_facts: false
  become: true
  roles:
     - role: swap
       tags: ['swap']
     - role: seedbox-mount
       tags: ['seedbox-mount']

    #- role: k3s_server
    #- role: k3s_upgrade