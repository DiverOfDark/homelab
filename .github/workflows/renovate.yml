name: Renovate

on:
  schedule:
    # Every 4 hours. Renovate itself is cheap (hits GitHub + registries only,
    # not the cluster); the cost driver is how many Renovate PRs each scan
    # produces, which is throttled by prHourlyLimit in renovate.json.
    - cron: "0 */4 * * *"
  workflow_dispatch:
    inputs:
      logLevel:
        description: "Log level"
        default: "info"
        type: choice
        options:
          - debug
          - info
          - warning

# Use GitHub-hosted runners deliberately. Self-hosted in-cluster runners are
# Talos/kubelet/Cilium round-tripped through etcd; Renovate would compete with
# the very cluster state it depends on for CI to validate its own PRs. Mend's
# free cloud tier OOM'd on this repo, so a hosted GitHub runner (7 GB) is the
# next step up that still costs nothing on the included Actions minutes.
permissions:
  contents: read

concurrency:
  group: renovate
  cancel-in-progress: false

jobs:
  renovate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Cache Renovate
        uses: actions/cache@v5
        with:
          path: /tmp/renovate
          key: renovate-cache

      # The renovate container internally runs as UID 12021. actions/cache
      # restores the tar as the runner user (UID 1001), so the directory tree
      # is owned by the wrong user and the container hits EACCES on mkdir.
      # Recommended fix straight from the renovatebot/github-action README.
      - name: Fix /tmp/renovate ownership for Renovate's container UID
        run: |
          sudo mkdir -p /tmp/renovate
          sudo chown -R 12021:0 /tmp/renovate

      - name: Self-hosted Renovate
        uses: renovatebot/github-action@v43.0.10
        with:
          token: ${{ secrets.RENOVATE_TOKEN }}
          configurationFile: renovate.json
        env:
          LOG_LEVEL: ${{ inputs.logLevel || 'info' }}
          RENOVATE_REPOSITORIES: ${{ github.repository }}
          RENOVATE_PLATFORM: github
          RENOVATE_ONBOARDING: "false"
          RENOVATE_REQUIRE_CONFIG: "required"
          RENOVATE_REPOSITORY_CACHE: "/tmp/renovate"