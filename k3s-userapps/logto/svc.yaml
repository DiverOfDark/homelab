apiVersion: v1
kind: Service
metadata:
  name: logto
spec:
  internalTrafficPolicy: Cluster
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 3001
  selector:
    app.kubernetes.io/pod: logto
  type: ClusterIP
