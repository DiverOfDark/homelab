apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: devpod-workspace
  namespace: devpod
spec:
  accessModes:
    - ReadWriteMany  # RWX - shared across all pods
  resources:
    requests:
      storage: 100Gi
  storageClassName: ceph-filesystem
